package com.example.dagger.dagger_lib

import com.example.dagger.core.AppObject

/**
 * This is a LibObject. Uses AppObject in some way, but it's exposed itself.
 */
class LibObject(private val appObject: AppObject)
