package com.example.dagger.dagger_lib

import com.example.dagger.core.AppObject

/**
 * This is a DaggerLibObject. Uses AppObject in some way, but it's exposed itself.
 */
class DaggerLibObject(private val appObject: AppObject)
